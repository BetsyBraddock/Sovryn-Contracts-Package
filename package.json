{
  "name": "@blobfishkate/sovryncontractswip",
  "version": "0.2.1",
  "description": "Contract addresses and ABIs used by the Sovryn Protocol (work in progress)",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "author": "blobfishkate",
  "license": "ISC",
  "repository": {
    "url": "https://github.com/BetsyBraddock/Sovryn-Contracts-Package.git"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "web3": "^1.4.0"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "ts-node": "^10.2.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.4.2"
  },
  "files": [
    "lib/**/*"
  ]
}